# Database Connection
# Required for both development and integration tests
DATABASE_URL=postgresql://user:password@localhost:5432/classlite

# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_STORAGE_BUCKET=gs://your-project.firebasestorage.app

# Server Configuration
PORT=4000

# Email Configuration (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM="ClassLite <no-reply@notifications.classlite.app>"

# Platform Admin
PLATFORM_ADMIN_API_KEY=your-admin-api-key

# Firebase Web API Key (for password verification via REST API)
# Get from Firebase Console > Project Settings > General > Web API Key
FIREBASE_API_KEY=your-firebase-web-api-key

# Inngest Configuration (Background Jobs)
# For local development, these are optional - Inngest Dev Server works without them
# For production, get these from https://app.inngest.com
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# AI Content Generation (Google Gemini)
# Required for AI question generation feature
# Get an API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
# Optional: Override default model (gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash
